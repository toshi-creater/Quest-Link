実装は以下手順でGitHubのIssueを優先度順に1つずつ実装してください。

各Issueの作業手順：
1. `feature/issue-{番号}-{概要}` の形式でブランチを作成する
2. 実装する
3. リモートへプッシュ前に確認する
4. 承認を得たらリモートへプッシュする
5. PRを作成する（タイトル：`feat: {Issueタイトル}`、本文にCloses #{番号} を記載する）
6. 完了を報告し、次のIssueへの着手前に確認を取る